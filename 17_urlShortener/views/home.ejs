<!DOCTYPE html>
<html lang="en" class="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>URL Shortener</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-slate-900 min-h-screen">
    <div class="container mx-auto px-4 py-12 max-w-4xl">
      <div class="text-center mb-16">
        <h1
          class="text-4xl font-bold bg-gradient-to-r from-orange-400 to-orange-600 bg-clip-text text-transparent"
        >
          URL Shortener
        </h1>
        <p class="mt-2 text-slate-400">
          Transform long links into short, shareable URLs
        </p>
      </div>

      <div
        class="bg-slate-800 rounded-xl p-8 shadow-2xl mb-12 border border-slate-700"
      >
        <form action="/url" method="POST" class="space-y-6">
          <div class="space-y-2">
            <label for="url" class="block text-sm font-medium text-slate-300">
              Enter your original URL
            </label>
            <div class="flex flex-col sm:flex-row gap-4">
              <input
                type="text"
                id="url"
                name="url"
                placeholder="https://example.com"
                class="w-full px-6 py-3 rounded-lg bg-slate-700 border border-slate-600 text-slate-200 placeholder-slate-500 focus:outline-none focus:ring-2 focus:ring-orange-500 focus:border-transparent transition-all"
              />
              <button
                type="submit"
                class="px-8 py-3 bg-orange-600 hover:bg-orange-700 text-white font-medium rounded-lg transition-all duration-300 transform hover:scale-[1.02] shadow-lg hover:shadow-orange-500/20 whitespace-nowrap"
              >
                ✨ Generate Short URL
              </button>
            </div>
          </div>
        </form>
      </div>

      <div
        class="bg-slate-800 rounded-xl shadow-2xl overflow-hidden border border-slate-700"
      >
        <div class="p-6 border-b border-slate-700">
          <h3 class="text-lg font-semibold text-slate-200">
            Recent Shortened URLs
          </h3>
        </div>

        <div class="overflow-x-auto">
          <table class="w-full">
            <thead class="bg-slate-750">
              <tr>
                <th
                  class="px-6 py-4 text-left text-sm font-medium text-slate-400"
                >
                  Original URL
                </th>
                <th
                  class="px-6 py-4 text-left text-sm font-medium text-slate-400"
                >
                  Short ID
                </th>
                <th
                  class="px-6 py-4 text-right text-sm font-medium text-slate-400"
                >
                  Clicks
                </th>
              </tr>
            </thead>
            <tbody class="divide-y divide-slate-700">
              <% if (urls && urls.length > 0) { %> <% urls.forEach(url => { %>
              <tr class="hover:bg-slate-850 transition-colors">
                <td class="px-6 py-4 whitespace-nowrap text-sm text-slate-300">
                  <%= url.redirectURL %>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-slate-300">
                  <%= url.shortId %>
                </td>
                <td
                  class="px-6 py-4 whitespace-nowrap text-right text-sm text-slate-300"
                >
                  <%= url.visitHistory ? url.visitHistory.length : 0 %>
                </td>
              </tr>
              <% }); %> <% } else { %>
              <tr>
                <td colspan="3" class="py-6 text-center text-slate-400">
                  No shortened URLs yet.
                </td>
              </tr>
              <% } %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </body>
</html>
